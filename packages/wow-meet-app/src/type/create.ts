import { type DateRange } from "react-day-picker";

export type Create = {
    title?: string;
    description?: string;
    dayList?: string[];
    dayRange?: DateRange;
    votesOpt?:string[];
  }

