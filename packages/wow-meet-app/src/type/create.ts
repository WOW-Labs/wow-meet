import { type DateRange } from "react-day-picker";

export type Create = {
    title?: string;
    description?: string;
    dayList?: string[];
    dateRange?: DateRange;
    votesOpt?:string[];
  }

